guard 'livereload' do
  extensions = {
    css: :css,
    scss: :css,
    sass: :css,
    js: :js,
    html: :html,
    png: :png,
    gif: :gif,
    jpg: :jpg,
    jpeg: :jpeg,
  }

  watch(%r{.+\.(html)})
  watch(%r{_sass/.+\.scss})
  watch(%r{_includes/.})
  watch(%r{_layouts/.})
end
